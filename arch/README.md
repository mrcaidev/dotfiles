# Arch

Bootstrap Arch Linux.

## 🚀 Getting Started

1. Use `fdisk` to format and partition the disk.
2. Run `bootstrap.sh` to bootstrap a basic Arch Linux system.
3. Run `arch-chroot /mnt` to enter the new system.
4. Run `personalization.sh` to personalize the system.
